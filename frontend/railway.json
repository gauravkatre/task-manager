{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build"
  },
  "deploy": {
    "startCommand": "node_modules/.bin/vite preview --port $PORT --host",
    "restartPolicyType": "ON_FAILURE"
  }
}